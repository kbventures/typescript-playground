// dfs iterative stack version
// 
function maxDepth(root) {
    if (!root)
        return 0;
    var maxDepth = 0;
    var stack = [root];
    while (stack.length > 0) {
    }
    return maxDepth;
}
;

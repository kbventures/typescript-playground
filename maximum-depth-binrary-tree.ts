
// dfs iterative stack version
// 
function maxDepth(root: TreeNode | null): number {
    if(!root) return 0;
    let maxDepth = 0;
    let stack = [root]
    
    while(stack.length > 0){

    }

    return maxDepth; 
};
